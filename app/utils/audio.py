import numpy as np

def extract_features(file_path: str):
    """
    TEMP MOCK FEATURE EXTRACTOR
    Returns dummy 1024-dim features (same as YAMNet output)
    """
    return np.zeros((1, 1024))
